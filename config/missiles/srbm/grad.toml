# System identification and description
[system]
# Official missile designation
name = "Grad"
# Full system name and role
description = "BM-21 Grad 122mm artillery rocket (Iron Dome's primary threat class)"
# Operating country/nation
country = "Russia"

# Missile classification
[classification]
# Type: ICBM (Intercontinental), IRBM (Intermediate), MRBM (Medium), SRBM (Short), SLBM (Sub-launched)
type = "SRBM"

# Range capabilities
[range]
# Minimum operational range (km) - below this, targeting may be impractical
min_range_km = 5.0
# Maximum range at optimal trajectory (km)
max_range_km = 40.0  # BM-21: ~20 km standard, ~40 km with rocket-assisted rounds

# Ballistic trajectory parameters - unguided artillery rocket, flat arc
[trajectory]
# Base apogee altitude (km) - trajectory peak altitude for minimum range shot
apogee_base_km = 5.0  # ~5 km at 20 km range (published BM-21 apogee); well inside Iron Dome's 10 km ceiling
# How much apogee increases per km of range (multiplier)
apogee_range_factor = 0.08  # ~8 km apogee at 40 km range
# Base flight time (seconds) for minimum range shot
flight_time_base_sec = 20.0
# How much flight time increases per km of range (multiplier)
flight_time_range_factor = 1.0  # ~20 s at 5 km, ~60 s at 40 km - slow, unguided

# Boost phase characteristics
[boost]
# Fraction of total flight time spent in powered boost phase (0.0-1.0)
boost_phase_fraction = 0.3
# Velocity at end of boost phase (km/s)
boost_velocity_km_s = 0.7  # BM-21 burnout velocity ~700 m/s

# Penetration aids and countermeasures
[countermeasures]
# Whether missile deploys decoys and chaff
has_countermeasures = false
# Number of decoys deployed in standard configuration
default_decoys = 0
# Maximum number of decoys that can be deployed
max_decoys = 0

# Radar cross section by flight phase
[radar_signature]
# RCS during boost phase (dBsm) - large due to exhaust plume
rcs_boost_dbsm = 10.0
# RCS during midcourse flight (dBsm) - unguided rocket, no RV shaping
rcs_midcourse_dbsm = 5.0
# RCS during terminal descent (dBsm) - still large; no separation event
rcs_terminal_dbsm = 3.0